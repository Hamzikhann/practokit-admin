import { PhoneNoDirective } from './phone-no.directive';

describe('PhoneNoDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNoDirective();
    expect(directive).toBeTruthy();
  });
});
