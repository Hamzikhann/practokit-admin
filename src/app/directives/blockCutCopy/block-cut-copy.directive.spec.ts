import { BlockCutCopyDirective } from './block-cut-copy.directive';

describe('BlockCutCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockCutCopyDirective();
    expect(directive).toBeTruthy();
  });
});
